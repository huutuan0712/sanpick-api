{
  "scripts": {
    "dev": "npx json-server data.json",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
